import{useState,useEffect,useRef}from"react";const RACES=[{id:"5k",label:"5K",miles:3.1,emoji:"🏃",weeks:8},{id:"10k",label:"10K",miles:6.2,emoji:"🏃",weeks:10},{id:"half",label:"Half Marathon",miles:13.1,emoji:"🌄",weeks:16},{id:"marathon",label:"Marathon",miles:26.2,emoji:"⛰️",weeks:20},{id:"50k",label:"50K Ultra",miles:31,emoji:"🏔️",weeks:24},{id:"50m",label:"50 Mile Ultra",miles:50,emoji:"🌲",weeks:28},{id:"100k",label:"100K Ultra",miles:62,emoji:"🦌",weeks:32},{id:"100m",label:"100 Mile Ultra",miles:100,emoji:"💀",weeks:36}];const LEVELS=[{id:"beginner",label:"Beginner",desc:"Running less than 6 months"},{id:"intermediate",label:"Intermediate",desc:"1–3 years of consistent running"},{id:"advanced",label:"Advanced",desc:"3+ years, multiple races completed"}];const FOCUSES=[{id:"finish",label:"Just Finish",icon:"🏁"},{id:"time",label:"Hit a Time Goal",icon:"⏱️"},{id:"podium",label:"Podium Contender",icon:"🥇"}];async function callCoach({messages,system}){const res=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages,system})});if(!res.ok)throw new Error("API error");const data=await res.json();return data.content?.map((b)=>b.text||"").join("")||"Keep pushing.";}function TypingText({text,speed=14}){const[displayed,setDisplayed]=useState("");const[done,setDone]=useState(false);useEffect(()=>{setDisplayed("");setDone(false);let i=0;const iv=setInterval(()=>{if(i<text.length){setDisplayed(text.slice(0,i+1));i++;}else{setDone(true);clearInterval(iv);}},speed);return()=>clearInterval(iv);},[text]);return<span>{displayed}{!done&&<span className="cursor">|</span>}</span>;}function Terrain(){return(<svg className="terrain" viewBox="0 0 1440 200" preserveAspectRatio="none"><defs><linearGradient id="tg" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2d5a27" stopOpacity="0.6"/><stop offset="100%" stopColor="#1a3518" stopOpacity="0.9"/></linearGradient></defs><path d="M0,180 C120,100 240,160 360,120 C480,80 600,150 720,100 C840,50 960,130 1080,90 C1200,50 1320,140 1440,80 L1440,200 L0,200 Z" fill="url(#tg)"/><path d="M0,200 C180,130 360,180 540,140 C720,100 900,160 1080,120 C1260,80 1380,160 1440,140 L1440,200 Z" fill="#0f2210" opacity="0.8"/></svg>);}function PulseRing({active}){if(!active)return null;return<div className="pulse-ring"><div className="ring r1"/><div className="ring r2"/><div className="ring r3"/></div>;}export default function App(){const[screen,setScreen]=useState("home");const[race,setRace]=useState(null);const[level,setLevel]=useState(null);const[focus,setFocus]=useState(null);const[messages,setMessages]=useState([]);const[input,setInput]=useState("");const[loading,setLoading]=useState(false);const[weekPlan,setWeekPlan]=useState(null);const[activeTab,setActiveTab]=useState("chat");const[step,setStep]=useState(0);const chatRef=useRef(null);const inputRef=useRef(null);useEffect(()=>{if(chatRef.current)chatRef.current.scrollTop=chatRef.current.scrollHeight;},[messages,loading]);const raceInfo=RACES.find((r)=>r.id===race);const levelInfo=LEVELS.find((l)=>l.id===level);const focusInfo=FOCUSES.find((f)=>f.id===focus);const coachSystem=`You are APEX, an elite ultra marathon and running coach. Athlete: ${levelInfo?.label}, training for ${raceInfo?.label} (${raceInfo?.miles} miles), goal: ${focusInfo?.label}. Training window: ${raceInfo?.weeks} weeks. Speak with authority and trail wisdom. Be specific. Use vivid language. Never be generic.`;const startCoach=async()=>{setScreen("coach");setActiveTab("chat");setLoading(true);try{const text=await callCoach({system:coachSystem,messages:[{role:"user",content:`Give a powerful personalized welcome to a ${levelInfo?.label} runner training for a ${raceInfo?.label}. Focus: ${focusInfo?.label}. 3-4 paragraphs, no headers.`}]});setMessages([{role:"assistant",content:text}]);}catch{setMessages([{role:"assistant",content:"Welcome, athlete. I'm APEX. Let's build something extraordinary."}]);}setLoading(false);};const sendMessage=async()=>{if(!input.trim()||loading)return;const userMsg=input.trim();setInput("");const newMessages=[...messages,{role:"user",content:userMsg}];setMessages(newMessages);setLoading(true);try{const text=await callCoach({messages:newMessages,system:coachSystem});setMessages([...newMessages,{role:"assistant",content:text}]);}catch{setMessages([...newMessages,{role:"assistant",content:"Connection lost on the trail. Try again."}]);}setLoading(false);};const generatePlan=async()=>{setActiveTab("plan");if(weekPlan)return;setLoading(true);try{const text=await callCoach({system:"You are an elite running coach. Return ONLY a valid JSON array, no markdown, no backticks, no explanation. Format: [{week,theme,totalMiles,days:[{day,workout,miles,type}]}]. Types: easy|tempo|long|rest|cross|intervals.",messages:[{role:"user",content:`4-week opening training block for ${levelInfo?.label} targeting ${raceInfo?.label} (${raceInfo?.miles}mi) in ${raceInfo?.weeks} weeks. Goal: ${focusInfo?.label}.`}]});setWeekPlan(JSON.parse(text.replace(/```json|```/g,"").trim()));}catch{setWeekPlan([]);}setLoading(false);};const typeColors={easy:"#4ade80",tempo:"#facc15",long:"#f97316",rest:"#64​​​​​​​​​​​​​​​​
